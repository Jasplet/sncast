[project]
name = "sncast"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.5",
    "pandas>=2.3.3",
    "pygc>=2.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
